// Funciones para abrir y cerrar el menu lateral

// Vamos a definir el ancho del menu cundo este abierto

const openNav = () => (document.getElementById("nav").style.width = "200px");

// Funcion para cerrar el menu lateral

const closeNav = () => (document.getElementById("nav").style.width = "0");
