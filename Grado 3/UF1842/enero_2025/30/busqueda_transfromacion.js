// indexOf(): Devuelve el primer índice en el que se encuentra un determinado elemento, o -1 si no lo encuentra.
let arrIndexOf = [23,45,75,32]

console.log("index de 75 dentro del arr, utilizando indexOf:", arrIndexOf.indexOf(75))

// lastIndexOf(): Devuelve el último índice en el que se encuentra un determinado elemento.


// includes(): Determina si un array contiene un determinado elemento.

// find(): Devuelve el primer elemento que cumple con la condición de la función de prueba.

// findIndex(): Devuelve el índice del primer elemento que cumple con la condición de la función de prueba.

// map(): Crea un nuevo array con los resultados de la función aplicada a cada elemento del array original.

// filter(): Crea un nuevo array con todos los elementos que pasen la prueba de la función dada.

// reduce(): Aplica una función a un acumulador y a cada elemento de un array (de izquierda a derecha), para reducirlo a un solo valor.

// reduceRight(): Similar a reduce(), pero procesa los elementos de derecha a izquierda.

// forEach(): Ejecuta una función en cada elemento de un array, pero no devuelve nada (solo efectos secundarios).